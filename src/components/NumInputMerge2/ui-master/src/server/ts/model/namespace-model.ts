export interface INamespace {
  clusterId?: string;
  clusterName?: string;
  clusterRegion?: string;
  created: string;
  id: string;
  key: string;
  name: string;
  updated: string;
  services?: any[];
}
