export function createPerfLogEntry(ctx, monitor: any, duration: number, success: boolean, error: boolean): void {
  // do nothing
}

export function storePerfMonitorEntry(monitor: any, duration: number): void {
  // do nothing
}
